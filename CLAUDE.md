# CLAUDE.md

이 파일은 이 저장소에서 작업할 때 Claude Code(claude.ai/code)에게 제공되는 안내 문서입니다.

## 프로젝트 현황

이 저장소에는 아직 애플리케이션 코드가 없습니다 — 현재는 `docs/` 아래에 기획 및 디자인 자료만 존재합니다. 빌드 시스템, 패키지 매니저 설정, 테스트 스위트가 없습니다. `docs/PLAN.md`에 기획 내용이 정리되어 있습니다. 구현이 시작되면 실제 빌드/린트/테스트 명령어와 아키텍처 관련 내용으로 이 파일을 업데이트해야 합니다.

## modooWA란

`docs/DESIGN.md`와 `docs/design-reference/`의 목업을 기반으로 볼 때, modooWA는 개발자와 컴플라이언스 담당자를 대상으로 한 웹 접근성(WCAG) 진단 도구입니다. 페이지/사이트를 감사하여 심각도별(치명/경고/권고)로 이슈를 보고하며, 코드 스니펫과 개선 권고 사항을 함께 제공합니다.
modooWA는 로그인이 필수인 서비스입니다. 로그인 후에만 URL 진단이 가능하며, 진단 결과는 자동 저장되지 않고 "저장하기" 버튼 클릭 시 폴더를 지정하며 저장됩니다.

참고 화면(파일명):

- 로그인화면(첫화면) — 로그인/첫 진입 화면
- 메인페이지 — 메인 대시보드
- 진단결과화면 — 진단 결과 화면
- 진단결과\_상세화면1 / 2 — 진단 결과 상세 화면
- 진단결과*저장하기*폴더선택화면 — 진단 결과 저장/폴더 선택 화면

### 페이지구조는 /docs/PLAN.md의 5. 화면 구성 (User Flow)을 따른다.

## 디자인 시스템 (`docs/DESIGN.md`)

`docs/DESIGN.md`는 시각 디자인의 기준 문서입니다 — YAML 프론트매터에 실제 디자인 토큰(색상, 타이포그래피, 라운드, 간격)이 정의되어 있고, 그 뒤에 사용 지침이 서술되어 있습니다. 프론트매터를 정확한 토큰 값으로 취급하고, 본문은 그 사용 의도와 규칙을 설명하는 것으로 이해하면 됩니다. UI 구현 시 핵심 사항:

- **브랜드 페르소나**: 권위 있고, 정밀하며, 정보 밀도가 높음 — "측정 도구" 같은 미학("언디자인": 그림자/그라데이션 대신 화이트 스페이스와 견고한 보더에 의존).
- **색상**: WCAG AAA 텍스트 대비 기준을 초과하도록 설계된 고대비 팔레트(접근성을 진단하는 도구라는 점과 부합). 주요 액션에는 프라이머리 블루 `#004ac6`, 헤더/네비게이션/헤드라인에는 딥 네이비 `#0F172A`. 심각도는 색상으로 엄격히 구분: 치명 `#DC2626`, 경고 `#D97706`, 권고 `#0891b2`.
- **타이포그래피**: 국문에는 Pretendard, 영문/숫자에는 Montserrat, 코드 스니펫/DOM 경로/기술 메타데이터에는 JetBrains Mono 사용. 헤딩은 딥 네이비 색상 사용. 본문 링크는 색맹 사용자를 위해 반드시 밑줄 표시(색상만으로 구분 금지).
- **레이아웃**: 사이드바 없는 풀 너비 구조 채택(자세한 예외사항은 "확정된 결정사항" 참고).
  본래 DESIGN.md 스펙은 12컬럼 고정 그리드 + 240px 사이드바 + 320px 인스펙터 패널이었으나 이번 프로젝트엔 미적용.
  태블릿은 8컬럼 유동 그리드; 모바일은 4컬럼 전체 너비 카드 스택. 수직 리듬은 8px 단위(조밀한 컴포넌트는 4px 단위); 카드 패딩은 24px 이상.
- **고도(Elevation)**: 그림자 대신 톤 레이어와 저대비(Slate-200) 아웃라인으로 깊이감 표현; 그림자는 툴팁/드롭다운 같은 일시적 오버레이에만 제한적으로 사용.
- **모양(Shapes)**: 버튼/입력 필드/카드 등 표준 컴포넌트는 4px 반경, 심각도 배지는 버튼과 시각적으로 구분되도록 2px 반경; 아이콘은 2px 선형 스트로크와 각진 끝(square caps) 사용.
- **초점 상태(Focus)**: 모든 대화형 요소는 명확하고 고대비인 `:focus-visible` 스타일이 필수 — 2px 화이트 오프셋을 포함한 2px 직선 프라이머리 블루. 이 제품의 목적상 타협 불가능한 항목.
- **문서화된 컴포넌트**: 버튼(Primary/Secondary/Ghost), 심각도 배지, 확장형 결과 카드(헤더에 심각도 배지 + 제목 + 코드 토글, 본문에 코드 스니펫 + 개선 권고), 수직 트리형 폴더 네비게이션(활성 항목은 왼쪽 2px 블루 보더로 표시), 입력 필드(레이블 항상 노출, 플로팅 레이블 금지, 에러 상태는 색상+아이콘 동시 표시).

UI 컴포넌트를 만들 때는 색상/크기를 임의로 정하지 말고 `docs/DESIGN.md` 프론트매터의 토큰 값을 그대로 사용하고, 전반에 걸쳐 AAA 대비 기준과 "색상만으로 정보를 전달하지 않는다"는 원칙을 지켜야 합니다 — 이 제품 자체가 스스로 진단하는 접근성 표준의 본보기가 되어야 하기 때문입니다.

## 프로젝트 컨텍스트

- 기획문서: docs/PLAN.md — 항상 이 문서 기준으로 기능 범위 판단
- 디자인 시스템: docs/DESIGN.md — 색상/타이포/간격/컴포넌트 스펙 원천
- 화면 참고: docs/design-reference/\*.jpg — v0 프로토타입 스크린샷 (레이아웃 구조만 참고, 색상은 무시)

## 확정된 결정사항

- 브랜드명: modooWA
- 테마: 라이트 테마 전체 통일 (DESIGN.md 기준, 스크린샷 다크 배경 무시)
- 레이아웃: 사이드바 없는 풀 너비 구조 (DESIGN.md 사이드바 스펙 미적용)

## 작업 원칙

- 모든 기능 작업은 Plan Mode로 먼저 계획 승인받고 진행
- 컴포넌트는 DESIGN.md 스펙을 벗어나지 않음
- 3개 이상 파일에 걸친 작업은 Explore → Plan → 승인 → Execute 순서로 진행

## Git/GitHub 작업 규칙

- git push, gh pr create, gh pr merge는 절대 실행하지 않는다 (권한상 차단되어 있음)
- 로컬 커밋까지 완료하면 작업을 멈추고, 사용자에게 "커밋 완료. push와 PR 생성은 직접 진행해주세요"라고 안내한다
- 커밋 메시지에는 관련 이슈 번호를 포함 (예: "feat: 버튼 컴포넌트 구현 (#2)")
